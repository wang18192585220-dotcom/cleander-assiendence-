# 🗓️ Google Calendar OAuth 配置保姆级教程

> 目标：让你的「个人智能日程规划助手」能够真正把任务写入 Google Calendar。
> 整个过程约 5-10 分钟，只需要做一次。

---

## 第 1 步：打开 Google Cloud Console

在浏览器打开这个链接：

👉 **https://console.cloud.google.com/projectselector2/home/dashboard**

你会看到一个页面，顶部有「选择项目」或「创建项目」的按钮。

### 如果这是你第一次用 Google Cloud：

1. 同意服务条款（勾选复选框，点「同意并继续」）
2. 页面会显示「选择项目」

---

## 第 2 步：创建新项目

1. 点击页面左上角或中间的 **「新建项目」** 按钮

   > 👆 通常在页面顶部，一个蓝色的按钮

2. 在弹出的窗口中：
   - **项目名称**：随便填，比如 `我的日程助手`
   - **位置**：不用改，默认即可
3. 点击 **「创建」**

4. 等待几秒钟，右上角会弹出通知「已创建项目」。点击通知里的 **「选择项目」**，或者等页面自动跳转。

   > 此时页面顶部应该显示你的项目名称，说明项目已创建并选中。

---

## 第 3 步：启用 Google Calendar API

1. 打开这个链接（在新标签页）：

   👉 **https://console.cloud.google.com/apis/library/calendar-json.googleapis.com**

2. 你会看到一个页面，标题是 **「Google Calendar API」**，中间有一个蓝色的大按钮。

3. 点击 **「启用」** 按钮。

   > ┌──────────────────────┐
   > │   Google Calendar API  │
   > │                        │
   > │   描述文字...           │
   > │                        │
   > │   [    启  用    ]     │  ← 点这个蓝色按钮
   > │                        │
   > └──────────────────────┘

4. 等待页面刷新，显示「API 已启用」即可。**这个标签页可以关掉了。**

---

## 第 4 步：创建 OAuth 客户端 ID（最关键的步骤）

1. 打开这个链接：

   👉 **https://console.cloud.google.com/apis/credentials**

   > 确保页面顶部的项目名称是你刚刚创建的。

2. 点击页面顶部的 **「+ 创建凭据」** 按钮，然后选择 **「OAuth 2.0 客户端 ID」**。

3. 这时可能出现「配置同意屏幕」的提示，**点进去先配置同意屏幕**：

   ### 4a. 配置同意屏幕（如果出现的话）

   1. 选择 **「外部」**（不是「内部」），点击「创建」
   2. 填写以下字段（**只需要填带 * 的**）：
      - **应用名称**：`我的日程助手`
      - **用户支持电子邮件**：选你的 Gmail
      - **开发者联系信息**：填你的 Gmail
   3. 其他所有字段**全部跳过**，直接滚动到底部，点击 **「保存并继续」**
   4. 接下来的「范围」页面，**什么都不用改**，直接点 **「保存并继续」**
   5. 接下来的「测试用户」页面：
      - 点击 **「+ ADD USERS」**
      - 输入你的 Gmail 地址
      - 点击「添加」
   6. 点击 **「保存并继续」**
   7. 最后点击 **「返回信息中心」**

   > 现在回到 credential 创建页面。如果没有自动回来，重新打开：
   > 👉 https://console.cloud.google.com/apis/credentials

4. 再次点击 **「+ 创建凭据」→「OAuth 2.0 客户端 ID」**

5. 在「应用类型」下拉框中，选择 **「桌面应用」**

   ```
   应用类型: [桌面应用        ▾]
   名称:      [桌面客户端 1      ]
   ```

6. 点击 **「创建」**

7. 弹出一个窗口显示你的客户端 ID。**点击右下角的「下载 JSON」**。

   ```
   ┌───────────────────────────┐
   │  OAuth 客户端已创建         │
   │                            │
   │  客户端 ID: xxxxx.apps...  │
   │  客户端密钥: xxxxxxxx      │
   │                            │
   │        [下载 JSON]         │  ← 点这个
   └───────────────────────────┘
   ```

8. 文件会下载到你的 **「下载」文件夹**，文件名类似：
   ```
   client_secret_XXXXX-XXXXX.apps.googleusercontent.com.json
   ```

---

## 第 5 步：把文件路径告诉 Hermes

把下载的 JSON 文件的**完整路径**发给我，例如：

```
我的文件在：/Users/wangshiyu/Downloads/client_secret_xxx.json
```

> 💡 在 Mac 上，你可以在 Finder 里右键点击文件 → 按住 Option 键 → 「拷贝路径名称」，然后粘贴给我。

---

## 第 6 步：授权（我会帮你完成）

把路径发给我之后，我会：
1. 运行授权命令
2. 给你一个 Google 授权链接
3. 你在浏览器打开那个链接，点击「允许」
4. 把浏览器地址栏的完整 URL 复制给我
5. 我完成最后的配置

**这一步之后，你的日程助手就能真正写入 Google Calendar 了！**

---

## 📋 快速检查清单

| 步骤 | 说明 | 状态 |
|------|------|------|
| 1 | 打开 Google Cloud Console | ☐ |
| 2 | 创建项目「我的日程助手」 | ☐ |
| 3 | 启用 Calendar API | ☐ |
| 4a | 配置同意屏幕（外部） | ☐ |
| 4b | 添加测试用户（你的 Gmail） | ☐ |
| 4c | 创建 OAuth 客户端 ID（桌面应用） | ☐ |
| 4d | 下载 JSON 文件 | ☐ |
| 5 | 把文件路径发给我 | ☐ |
| 6 | 我来完成授权 | ☐ |

---

> 如果过程中卡住了，随时告诉我你在哪一步，看到什么页面，我来帮你。
